<h1>Signup Form</h1>

<%= form_for @signup do |f|%>
<p>Choose camper:<%= f.collection_select :camper_id, Camper.all, :id, :name%></p>
<p>Choose activity:<%= f.collection_select :activity_id, Activity.all, :id, :name%></p>
<p><%=f.label :time%>
<p><%= f.number_field :time%>
<%= f.submit%>
<%end%>
